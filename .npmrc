# PNPM Configuration for Vercel
auto-install-peers=true
strict-peer-dependencies=false

# Allow build scripts for required dependencies
unsafe-perm=true

# Optimize for production builds
prefer-frozen-lockfile=true